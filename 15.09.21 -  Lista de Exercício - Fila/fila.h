#include "lista.h"

#include <iostream>
#include <stdlib.h>

Lista* inicializa ();

Lista* enqueue(Lista* l, int i);
void dequeue (Lista* l);
void front (Lista* l);
void isempty(Lista* l);
void size (Lista* l);
